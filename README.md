## TIER IV universe / split mirror (common)

This repository split mirrors `main` branch of `autowarefoundation/autoware_universe:/common/` into `awf-latest` branch, used for TIER IV release workflows.

This branch only holds workflow files. See the beta branches for source codes.
